# SiriusDV AI Assistant - Техническое Задание (ТЗ)

[# SiriusDV AI Assistant - Техническое Задание (ТЗ)

**Проект:** SiriusDV - AI-ассистент для поиска автомобилей из Япони/Кореи  
**GitHub:** https://github.com/Nestasss/sirius-dv-assistant  
**VPS:** root@5928827-mp035305 (notificbot.ru)  
**Стек:** n8n + PostgreSQL + Playwright + Claude (Llama 4 Maverick via OpenRouter)  
**Дата начала:** 17 ноября 2025  
**Статус:** ✅ Backend готов на 80%, Frontend в процессе

---

# 📋 ЭТАП 1: ЧТО УЖЕ СДЕЛАНО ✅

## 1.1 GitHub Репозиторий

- ✅ Создан публичный репо: https://github.com/Nestasss/sirius-dv-assistant
- ✅ Структура проекта:
  ```
  sirius-dv-assistant/
  ├── SQL/schema.sql                    # PostgreSQL схема (10 таблиц)
  ├── scraper/
  │   ├── encar-scraper.js              # Парсинг Encar.com (Корея)
  │   └── idubid-scraper.js             # Парсинг iDubid.com (Корея)
  ├── calculator/
  │   └── price-calculator.js           # Расчёт полной стоимости
  ├── n8n/
  │   ├── sirius-workflow.json          # Основной workflow
  │   └── exchange-rates-workflow.json  # Обновление курсов валют
  ├── docs/
  │   ├── DEPLOYMENT.md                 # Гайд деплоя
  │   └── QUICK_START.md                # Быстрый старт
  ├── README.md                         # Описание проекта
  └── .gitignore                        # Исключения для гита
  ```

## 1.2 PostgreSQL База Данных

**Подключение:**
- Host: n8n-docker-postgres-1
- User: n8n
- Database: n8n
- Port: 5432

**Созданные таблицы (10 штук):**

| Таблица | Назначение | Статус |
|---------|-----------|--------|
| car_listings | Объявления авто с Encar/iDubid | ✅ Создана, готова к заполнению |
| japan_fees | Комиссии Японии (ПРР, Растаможивание, Фрахт) | ✅ Заполнена (3 записи) |
| utilization_fees | Таблица утильсборов по объёму | ✅ Создана, пуста (для заполнения) |
| customs_duties | Таможенные пошлины по странам | ✅ Заполнена (9 записей: Japan/Korea/China) |
| shipping_prices | Цены доставки по регионам России | ✅ Создана, пуста (для заполнения) |
| exchange_rates | Курсы валют (JPY, KRW, CNY → RUB) | ✅ Создана, пуста (обновляется автоматически) |
| user_requests | Заявки клиентов | ✅ Создана, готова к использованию |
| knowledge_base | FAQ по компании | ✅ Заполнена (4 записи) |
| chat_history | История чатов пользователей | ✅ Создана, пуста |
| operation_logs | Логирование операций | ✅ Создана, пуста |

**Данные в БД:**
```sql
-- FAQ (4 записи)
SELECT * FROM knowledge_base;
-- Результат: компания, доставка, комиссии, контакты

-- Japan Fees (3 записи)
SELECT * FROM japan_fees;
-- ПРР + СБКТС: ¥45,500
-- Растаможивание: ¥45,000
-- Фрахт: ¥250,000

-- Customs Duties (9 записей)
SELECT * FROM customs_duties;
-- Japan: 15% для всех возрастов
-- Korea: 15% для всех возрастов
-- China: 48% для всех возрастов
```

## 1.3 Scrapers (Playwright)

### 1.3.1 Encar Scraper (`scraper/encar-scraper.js`)

**Функции:**
- `launch()` - запуск браузера
- `search(make, model, year, maxResults=3)` - поиск авто на Encar.com
- `getDetails(listingUrl)` - получение деталей объявления
- `close()` - закрытие браузера

**Парсит:**
- Марка и модель
- Цена в ₩ (корейских вонах)
- Пробег (км)
- Тип кузова
- Руль (левый/правый)
- Главное фото
- Ссылка на объявление

**Статус:** ✅ Готов к использованию в n8n

### 1.3.2 iDubid Scraper (`scraper/idubid-scraper.js`)

**Функции:** аналогично Encar  
**Отличия:** парсит сайт iDubid.com (альтернативный корейский аукцион)  
**Статус:** ✅ Готов к использованию в n8n

## 1.4 Price Calculator (`calculator/price-calculator.js`)

**Класс PriceCalculator:**

**Методы:**
- `constructor(exchangeRates)` - инициализация с курсами валют
- `convertCurrency(amount, from, to)` - конвертация валют (JPY/KRW → RUB)
- `calculateTotal(carData)` - расчёт полной стоимости
- `updateExchangeRates(rates)` - обновление курсов

**Формула расчёта:**
```
1. Цена на аукционе (¥ или ₩)
2. + Комиссия брокера (6% от цены)
3. + ПРР + СБКТС (¥45,500)
4. + Растаможивание (¥45,000)
5. + Фрахт (¥250,000)
─────────────────────────────
= ИНВОЙС (конвертируем в RUB)

6. + Утильсбор (от 3,400 руб для физлиц)
7. + Таможенная пошлина (15% от цены авто)
8. + НДС (18% от суммы)
9. + Доставка по России (в выбранный регион)
─────────────────────────────
= ИТОГОВАЯ СТОИМОСТЬ
```

**Пример вывода:**
```javascript
{
  auctionPrice: { jpy: 2400000, rub: 156000, usd: 1400 },
  invoice: { jpy: 3370500, rub: 219480 },
  fees: { utilFee: 667400, customsDuty: 23400, nds: 110316 },
  total: 420596 // итого ≈ 420 тысяч рублей
}
```

**Статус:** ✅ Готов к использованию

## 1.5 n8n Webhook

**URL:** https://notificbot.ru/webhook/sirius-assistant  
**Метод:** POST  
**Статус:** ✅ **РАБОТАЕТ** (протестировано curl, HTTP/2 200)

**Тестовый запрос:**
```bash
curl -X POST "https://notificbot.ru/webhook/sirius-assistant" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Ищу Honda CR-V 2020",
    "user_id": "test123"
  }'
```

**Результат:** ✅ `{"message":"Workflow was started"}`

---

# 🚧 ЭТАП 2: ЧТО НУЖНО СДЕЛАТЬ (TODO)

## 2.1 Заполнить оставшиеся таблицы БД

### 2.1.1 Таблица `utilization_fees` (утильсборы)

**Что делать:**
```bash
docker exec -it n8n-docker-postgres-1 psql -U n8n -d n8n
```

**Вставить данные:**
```sql
-- Утильсборы для физлиц (зависит от объёма и возраста)
INSERT INTO utilization_fees 
(category, engine_volume_from, engine_volume_to, power_hp_from, power_hp_to, 
 condition, fee_rub, country, apply_to) VALUES

-- 1-2л
('1-2л', 1.0, 2.0, 50, 160, 'new', 667400, 'Japan', 'physical_person'),
('1-2л', 1.0, 2.0, 50, 160, 'used', 1174000, 'Japan', 'physical_person'),

-- 2-3л
('2-3л', 2.0, 3.0, 150, 200, 'new', 837000, 'Japan', 'physical_person'),
('2-3л', 2.0, 3.0, 150, 200, 'used', 1240800, 'Japan', 'physical_person'),

-- 3-4л
('3-4л', 3.0, 4.0, 200, 250, 'new', 1004200, 'Japan', 'physical_person'),
('3-4л', 3.0, 4.0, 200, 250, 'used', 1407600, 'Japan', 'physical_person'),

-- Аналогично для Korea и China (пошлины выше)
...
```

**Проверка:**
```sql
SELECT COUNT(*) FROM utilization_fees;
-- Должно быть не менее 18 записей
```

### 2.1.2 Таблица `shipping_prices` (доставка по регионам)

**Что делать:** Загрузить CSV с доставкой

```bash
# Из файла dostavka_avto_iz_vladivostoka.csv
docker exec -it n8n-docker-postgres-1 psql -U n8n -d n8n

-- Вставить примеры:
INSERT INTO shipping_prices (region, distance_km, car_type, price_rub) VALUES
('Хабаровск', 1220, 'кей-кар', 25000),
('Благовещенск', 1380, 'кей-кар', 35000),
('Москва', 9300, 'кей-кар', 155000),
('СПб', 9900, 'кей-кар', 160000),
('Екатеринбург', 3760, 'кей-кар', 110000),
...
```

**Источник:** Файл `dostavka_avto_iz_vladivostoka.csv`

## 2.2 n8n Workflow - Финальная настройка

### 2.2.1 Текущий статус

- ✅ Webhook создан и работает
- ⚠️ Code Node создан (базовый)
- ❌ AI Chat Node НЕ подключен
- ❌ Response Node НЕ настроен

### 2.2.2 Что нужно сделать

**Шаг 1: Откройте n8n UI**
```
https://notificbot.ru
Логин: admin
Пароль: (из .env файла)
```

**Шаг 2: Отредактируйте Code Node**

```javascript
const body = $input.all()[0].json.body || $input.all()[0].json;
return [{
  json: {
    message: body.message,
    user_id: body.user_id || 'anonymous',
    chatInput: body.message
  }
}];
```

**Шаг 3: Добавьте HTTP Request Node (для OpenRouter API)**

```
Method: POST
URL: https://openrouter.ai/api/v1/chat/completions

Headers:
Authorization: Bearer sk-or-v1-YOUR_OPENROUTER_KEY
Content-Type: application/json

Body (JSON):
{
  "model": "meta-llama/llama-4-maverick",
  "messages": [
    {
      "role": "system",
      "content": "Ты AI-ассистент SiriusDV. Помогаешь найти авто из Японии/Кореи..."
    },
    {
      "role": "user",
      "content": "{{ $json.message }}"
    }
  ],
  "max_tokens": 500
}
```

**Шаг 4: Добавьте Respond to Webhook Node**

```
Response Code: 200
Response Body:
{{ $json.choices[0].message.content }}
```

**Шаг 5: Активируйте Workflow**
- Нажмите кнопку "Active" (вверху справа)
- Статус должен быть "Active"

**Шаг 6: Тестируйте**
```bash
curl -X POST "https://notificbot.ru/webhook/sirius-assistant" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Ищу Toyota Camry 2019",
    "user_id": "user_123"
  }'
```

**Ожидаемый результат:**
```json
{
  "message": "Я найду для вас Toyota Camry 2019... (ответ от AI)"
}
```

## 2.3 Интеграция Scrapers в n8n

**Статус:** ❌ Не начинали

### 2.3.1 Подход 1: Прямой запуск Playwright из n8n

**Что нужно:**
1. Установить Playwright в Docker контейнер n8n
2. Создать Code Node, который вызывает scraper
3. Парсить результаты и сохранять в БД

**Команды:**
```bash
# На VPS
docker exec n8n-docker-n8n-1 npm install playwright

# В n8n Code Node:
const EncarScraper = require('./scraper/encar-scraper.js');
const scraper = new EncarScraper();
await scraper.launch();
const results = await scraper.search('Honda', 'CR-V', 2020, 3);
await scraper.close();
return [{ json: { listings: results } }];
```

### 2.3.2 Подход 2: Отдельный Node.js сервис (рекомендуется)

Создать REST API для scrapers и вызывать через HTTP Request node.

## 2.4 Telegram Интеграция

**Статус:** ❌ Не начинали

### 2.4.1 Создать Telegram Bot Credential

1. Откройте n8n UI
2. Settings → Credentials → New
3. Выберите "Telegram"
4. Вставьте токен бота

**Где получить токен?**
```
BotFather в Telegram → /newbot → скопируйте token
```

### 2.4.2 Добавить Telegram Node в workflow

```
Method: Send Message
Chat ID: -YOUR_MANAGER_CHAT_ID
Message:
🔥 НОВЫЙ ЗАПРОС
👤 Пользователь: {{ $json.user_id }}
💬 Запрос: {{ $json.message }}
```

## 2.5 Память ассистента (Chat History)

**Статус:** ❌ Не начинали

**Что нужно:**
1. При каждом запросе сохранять в `chat_history` таблицу
2. При следующем запросе от того же пользователя загружать историю
3. Передавать историю в AI prompt

**Реализация:**
- PostgreSQL Node → SELECT * FROM chat_history WHERE user_id = ...
- Добавить контекст в system message AI
- После ответа → INSERT в chat_history

## 2.6 PWA Frontend (Lovable или React)

**Статус:** ❌ Не начинали

### 2.6.1 Вариант A: Lovable (no-code, быстро)

1. Откройте https://lovable.dev
2. Создайте новый проект
3. Промпт:
```
Создай красивое PWA для "SiriusDV" - поиска авто из Японии/Кореи.

Функции:
1. Поле ввода для поиска (Honda CR-V, Toyota Camry и т.д.)
2. Вывод результатов (карточки с авто)
3. Интеграция с webhook: https://notificbot.ru/webhook/sirius-assistant
4. Loading spinner при поиске
5. Кнопка "Записаться на консультацию"
6. Форма: ФИО, Телефон, Телеграм, Бюджет

Дизайн:
- Темная тема (чёрный/серый)
- Оранжевые/жёлтые акценты
- Логотип SiriusDV в шапке
- Адаптивный дизайн для мобилок
```

4. Экспортируйте код → деплоймите на Vercel

### 2.6.2 Вариант B: React (более контролируемый)

**Структура:**
```
frontend/
├── src/
│   ├── components/
│   │   ├── SearchForm.jsx       # Форма поиска
│   │   ├── CarCard.jsx          # Карточка авто
│   │   └── ConsultationForm.jsx # Форма консультации
│   ├── services/
│   │   └── api.js               # Вызовы к webhook
│   ├── App.jsx
│   └── index.css
├── package.json
└── vite.config.js
```

**Основные компоненты:**

**SearchForm.jsx:**
```jsx
const [query, setQuery] = useState('');
const [results, setResults] = useState([]);
const [loading, setLoading] = useState(false);

const handleSearch = async () => {
  setLoading(true);
  const response = await fetch('https://notificbot.ru/webhook/sirius-assistant', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: query, user_id: 'anonymous' })
  });
  const data = await response.json();
  setResults(data.listings || []);
  setLoading(false);
};

return (
  <div>
    <input value={query} onChange={(e) => setQuery(e.target.value)} />
    <button onClick={handleSearch} disabled={loading}>
      {loading ? 'Ищу...' : 'Поиск'}
    </button>
  </div>
);
```

---

# 🎯 ПЛАН РАБОТ (Приоритет)

## Неделя 1 (Критично)

- [ ] 2.1.1 Заполнить `utilization_fees` в БД (2 часа)
- [ ] 2.1.2 Заполнить `shipping_prices` в БД (1 час)
- [ ] 2.2 Настроить n8n workflow полностью (3 часа)
  - [ ] HTTP Request Node для OpenRouter
  - [ ] Respond to Webhook Node
  - [ ] Тестирование
- [ ] **Результат:** Полностью рабочий webhook → AI Chat → Ответ

## Неделя 2 (Важно)

- [ ] 2.3 Интегрировать Scraper в n8n (4-5 часов)
  - [ ] Вариант 1 или 2 из раздела 2.3
  - [ ] Тестирование парсинга
- [ ] **Результат:** Webhook → Scraper → Карточки авто

## Неделя 3 (Улучшения)

- [ ] 2.4 Telegram уведомления менеджерам (2 часа)
- [ ] 2.5 Chat History (память ассистента) (3 часа)
- [ ] **Результат:** Менеджеры видят запросы, контекст сохраняется

## Неделя 4 (Frontend)

- [ ] 2.6 Создать PWA (Lovable или React) (6-8 часов)
- [ ] Деплой на Vercel
- [ ] Интеграция с webhook
- [ ] **Результат:** Полнофункциональное приложение

---

# 📊 Структура Данных

## Webhook Request Format

```json
{
  "message": "Ищу Honda CR-V 2020",
  "user_id": "telegram_12345",
  "chatInput": "Ищу Honda CR-V 2020"
}
```

## Webhook Response Format

```json
{
  "message": "AI-ответ",
  "listings": [
    {
      "make": "Honda",
      "model": "CR-V",
      "year": 2020,
      "title": "Honda CR-V 2020 автомат",
      "price_krw": 23000000,
      "price_rub": "≈150,000",
      "mileage": 120000,
      "body_type": "кроссовер",
      "steering": "левый",
      "photo_url": "https://...",
      "listing_url": "https://encar.com/..."
    }
  ],
  "total_price": 450000,
  "breakdown": {
    "invoice": 219480,
    "utilFee": 667400,
    "customsDuty": 23400,
    "nds": 110316,
    "delivery": 155000
  }
}
```

---

# 🔧 Ресурсы и Ключи

## Требуемые API ключи (для .env)

```bash
# OpenRouter (AI)
OPENROUTER_API_KEY=sk-or-v1-... 
# Получить: https://openrouter.ai

# Telegram Bot
TELEGRAM_BOT_TOKEN=...
MANAGER_CHAT_ID=-...
# Получить: BotFather в Telegram

# OpenExchangeRates (курсы валют)
EXCHANGE_RATES_API_KEY=...
# Получить: https://openexchangerates.org
```

## GitHub Репо

```bash
git clone https://github.com/Nestasss/sirius-dv-assistant.git
cd sirius-dv-assistant
git pull origin main  # всегда синхронизироваться перед началом
```

## VPS Доступ

```bash
ssh root@5928827-mp035305

# Папка проекта
cd ~/sirius-dv-assistant

# n8n UI
https://notificbot.ru

# PostgreSQL
docker exec -it n8n-docker-postgres-1 psql -U n8n -d n8n
```

---

# ✅ Контрольный Список (Verification Checklist)

## На конец каждого этапа проверяйте:

### Этап 1 (БД)
- [ ] Все 10 таблиц в PostgreSQL
- [ ] FAQ заполнена (4+ записи)
- [ ] Japan Fees заполнена (3 записи)
- [ ] Customs Duties заполнена (9 записей)
- [ ] Utilization Fees заполнена (18+ записей)
- [ ] Shipping Prices заполнена (10+ записей)

### Этап 2 (n8n Workflow)
- [ ] Webhook получает POST запросы (200)
- [ ] Code Node парсит JSON корректно
- [ ] HTTP Request Node вызывает OpenRouter API
- [ ] AI ChatModel возвращает текст
- [ ] Respond Node отправляет ответ клиенту
- [ ] Workflow помечен как "Active"

### Этап 3 (Scrapers)
- [ ] Playwright установлен в n8n контейнер
- [ ] Scraper успешно парсит Encar
- [ ] Scraper успешно парсит iDubid
- [ ] Результаты сохраняются в `car_listings` таблицу

### Этап 4 (Frontend)
- [ ] PWA работает на мобилке и десктопе
- [ ] Форма поиска отправляет запрос на webhook
- [ ] Результаты отображаются в карточках
- [ ] Кнопка консультации работает

---

# 📞 Контакты и Информация о Компании

**SiriusDV (Сириус Дальний Восток)**
- Опыт: 15+ лет
- Услуги: Покупка авто из Японии, Кореи, Китая, ОАЭ, Грузии
- Доставка: до Владивостока (3 недели), затем по России
- Телефон: 8 800 101 50 86
- Адрес: г. Владивосток, ул. Стрельникова, 7, офис 708А

**Менеджеры (для Telegram уведомлений):**
- Анжелика Андриянова (ГД): +7 953 202-83-30
- Роман (Зам. ГД): +7 963 515-55-06
- Email: manager@sirius.ru

---

# 📝 Заметки для следующей сессии

1. **Если продолжаете работу на новом аккаунте:**
   - Всё находится в GitHub репо
   - VPS IP и логины выше
   - Все ключи API в .env файле на VPS

2. **Быстрый старт:**
   ```bash
   ssh root@5928827-mp035305
   cd ~/sirius-dv-assistant
   git pull
   docker exec -it n8n-docker-postgres-1 psql -U n8n -d n8n
   ```

3. **Приоритет сейчас:** Заполнить БД и настроить n8n workflow полностью

4. **Общее время на проект (оценка):**
   - Неделя 1: 6 часов
   - Неделя 2: 5 часов
   - Неделя 3: 5 часов
   - Неделя 4: 8 часов
   - **Итого:** ~24 часа на полный проект

---

**Документ подготовлен:** 17 ноября 2025  
**Версия:** 1.0  
**Ответственный:** Stanislav (Nestasss на GitHub)
]

